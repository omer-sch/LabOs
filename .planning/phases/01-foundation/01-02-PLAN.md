---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - prisma/schema.prisma
  - lib/prisma.ts
  - prisma/migrations/
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "All tables exist in PostgreSQL after migration (users, cycle_tests, cycle_data, ai_analyses, experiments, experiment_cycle_tests, production_projects, slurry_runs, mixing_steps, electrode_runs, assembly_runs)"
    - "JSONB metadata columns exist on SlurryRun, ElectrodeRun, AssemblyRun, CycleDatum"
    - "Soft-delete deletedAt columns exist on CycleTest, Experiment, ProductionProject"
    - "lib/prisma.ts singleton is the only place PrismaClient is instantiated"
    - "prisma generate succeeds with zero errors"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Complete schema — all four pillars, all tables"
      contains: "model CycleTest"
    - path: "lib/prisma.ts"
      provides: "Prisma singleton"
      exports: ["prisma"]
  key_links:
    - from: "prisma/schema.prisma"
      to: "DATABASE_URL / DIRECT_URL"
      via: "datasource db block"
      pattern: "directUrl"
    - from: "lib/prisma.ts"
      to: "globalThis"
      via: "singleton guard"
      pattern: "globalForPrisma"
---

<objective>
Write the complete Prisma schema covering all four pillars (cycle testing, experiments, manufacturing, auth) with JSONB metadata columns, soft-delete columns, and FK constraints. Run the initial migration. Create the lib/prisma.ts singleton.

Purpose: Every future phase imports from lib/prisma.ts and relies on the schema being complete. Schema gaps discovered later require expensive backfill migrations.
Output: prisma/schema.prisma (all tables), lib/prisma.ts (singleton), applied migration in PostgreSQL.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Prisma singleton pattern (from RESEARCH.md) -->
```typescript
// lib/prisma.ts
import { PrismaClient } from "@prisma/client";
const globalForPrisma = globalThis as unknown as { prisma: PrismaClient };
export const prisma =
  globalForPrisma.prisma ||
  new PrismaClient({
    log: process.env.NODE_ENV === "development" ? ["query", "error", "warn"] : ["error"],
  });
if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;
```

<!-- Schema datasource block (from RESEARCH.md) -->
```prisma
datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write complete Prisma schema and create lib/prisma.ts singleton</name>
  <files>prisma/schema.prisma, lib/prisma.ts</files>
  <action>
    Run `npx prisma init` if prisma/schema.prisma does not exist yet.

    Replace the entire contents of `prisma/schema.prisma` with the complete schema from RESEARCH.md (the "Complete Prisma Schema" code block). This schema includes ALL tables:
    - User (Clerk user ID as @id, email unique, name)
    - CycleTest (with deletedAt soft-delete, createdById FK to User)
    - CycleDatum (with metadata Json? JSONB column, index on [cycleTestId, cycleNumber])
    - AIAnalysis (one-to-one with CycleTest)
    - Experiment (with conditions Json, materials Json, deletedAt soft-delete, ExperimentStatus enum)
    - ExperimentCycleTest (composite PK join table)
    - ProductionProject (with all boolean flags: anodeSlurryDone, cathodeSlurryDone, electrodeAnodeDone, electrodeCathodeDone, assemblyDone, cellsDone; deletedAt soft-delete; ProjectType enum)
    - SlurryRun (with viscosityMeasurements Json?, metadata Json? JSONB)
    - MixingStep
    - ElectrodeRun (with metadata Json? JSONB)
    - AssemblyRun (with metadata Json? JSONB; ElectrodeSource enum)
    - Enums: ExperimentStatus, ProjectType, ElectrodeType, ElectrodeSource

    The datasource block MUST include both `url = env("DATABASE_URL")` and `directUrl = env("DIRECT_URL")`.

    Create `lib/prisma.ts` with the singleton pattern shown in the interfaces block above. Add `import "server-only"` as the first line to prevent accidental client-side imports.

    Note: `import "server-only"` requires the `server-only` package: `npm install server-only`.
  </action>
  <verify>
    <automated>cd /Users/omer/Desktop/battery-system && npx prisma validate 2>&1</automated>
  </verify>
  <done>`npx prisma validate` exits 0 with no errors. All tables present in schema. Both url and directUrl declared in datasource block.</done>
</task>

<task type="auto">
  <name>Task 2: Run initial migration and verify all tables in PostgreSQL</name>
  <files>prisma/migrations/</files>
  <action>
    Run the initial migration:
    ```
    npx prisma migrate dev --name init
    ```

    This uses DIRECT_URL (not the pooler) for the migration connection — Prisma handles this automatically when directUrl is set.

    After migration, verify tables exist:
    ```
    npx prisma studio
    ```
    Or use psql to count tables:
    ```
    npx prisma db execute --stdin <<< "SELECT tablename FROM pg_tables WHERE schemaname='public' ORDER BY tablename;"
    ```

    Run `npx prisma generate` to generate the Prisma Client types.

    Verify the singleton compiles by running a quick TypeScript check:
    ```
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>cd /Users/omer/Desktop/battery-system && npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM pg_tables WHERE schemaname='public';" 2>&1</automated>
  </verify>
  <done>
    Migration applied. At least 11 tables exist in the public schema (users, cycle_tests, cycle_data, ai_analyses, experiments, experiment_cycle_tests, production_projects, slurry_runs, mixing_steps, electrode_runs, assembly_runs). `npx prisma generate` succeeds. `npx tsc --noEmit` passes.
  </done>
</task>

</tasks>

<verification>
- `npx prisma validate` exits 0
- `npx prisma generate` succeeds (no type errors)
- `npx tsc --noEmit` passes
- All 11+ tables present in PostgreSQL public schema
- `lib/prisma.ts` has `import "server-only"` as first line
- JSONB metadata columns on SlurryRun, ElectrodeRun, AssemblyRun, CycleDatum
- deletedAt on CycleTest, Experiment, ProductionProject
</verification>

<success_criteria>
Complete schema migrated to local PostgreSQL. Prisma singleton in lib/prisma.ts. TypeScript compiles cleanly. Ready for Plan 03 (Clerk auth).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with:
- Migration name and timestamp
- List of all tables created
- Confirmation that JSONB and soft-delete columns are present
- Any schema adjustments made during implementation
</output>
